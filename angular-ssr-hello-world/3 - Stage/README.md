# Angular SSR Hello World - Stage Environment

This is [a stage environment](https://app.zerops.io/recipes/angular-ssr-hello-world?environment=stage) for [Angular SSR Hello World (info + deploy)](https://app.zerops.io/recipes/angular-ssr-hello-world?environment=stage) recipe on [Zerops](https://zerops.io).

<!--#ZEROPS_EXTRACT_START:intro#-->
**Stage** environment uses the same configuration as production, but runs on a single container with lower scaling settings.
<!--#ZEROPS_EXTRACT_END:intro#-->
