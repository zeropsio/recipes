# 2 — Local

Single production service for local development workflows.

- Run Laravel locally, deploy to Zerops with `zcli push`.
- Only one app service (prod) — no dev/prod split needed for local work.
- Database runs in **NON_HA** mode (single node).
- Suitable for developers who prefer local editors and tools with Zerops as the runtime.
