# 5 — Highly-available Production

Full HA production environment with dedicated resources.

- Database runs in **HA** mode (multi-node with automatic failover).
- Minimum **2 app containers** with dedicated CPU (not shared).
- Uses the **SERIOUS** core package for guaranteed resource allocation.
- Designed for production workloads requiring high availability and consistent performance.
