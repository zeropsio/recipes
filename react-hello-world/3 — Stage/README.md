# React Hello World — Stage Environment

This is a stage environment for [React Hello World (info + deploy)](https://app.zerops.io/recipes/react-hello-world?environment=stage) recipe on [Zerops](https://zerops.io).

<!-- #ZEROPS_EXTRACT_START:intro# -->
**Stage** environment uses the same configuration as production, but runs on a single container with lower scaling settings.
<!-- #ZEROPS_EXTRACT_END:intro# -->
