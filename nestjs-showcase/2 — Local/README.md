# NestJS Showcase — Local Environment
This is a local environment for [NestJS Showcase (info + deploy)](https://app.zerops.io/recipes/nestjs-showcase?environment=local) recipe on [Zerops](https://zerops.io).

<!-- #ZEROPS_EXTRACT_START:intro# -->
**Local** environment provides a single-slot deploy of the recipe — no paired dev runtime, just one `api` + `app` + `worker` against the same managed services. Use it to mirror production shape locally without paying for the dev/stage pair.
<!-- #ZEROPS_EXTRACT_END:intro# -->
