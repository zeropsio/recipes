# React Router v7 Hello World — Stage Environment

This is a stage environment for [React Router v7 Hello World (info + deploy)](https://app.zerops.io/recipes/reactrouterv7-static-hello-world?environment=stage) recipe on [Zerops](https://zerops.io).

<!-- #ZEROPS_EXTRACT_START:intro# -->
**Stage** environment uses the same configuration as production, but runs on a single container with lower scaling settings.
<!-- #ZEROPS_EXTRACT_END:intro# -->
